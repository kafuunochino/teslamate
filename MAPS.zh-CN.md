# 地图设置

管理员打开“系统设置 → 地图”，选择地图提供商，填写对应配置，
再点击“保存并应用”。选择或填写过程中不会改变正在使用的地图。
配置保存在数据库中，应用或 VPS 重启后继续生效。

## 高德地图

适用于国内网络。首页实时位置、行车仪表盘、行程轨迹和地理围栏使用高德
JS API 2.0，地图跟随页面切换深浅色，不需要单独设置地图颜色。

1. 在[高德开放平台](https://console.amap.com/dev/key/app)创建应用，
   添加服务平台为 **Web 端（JS API）** 的 Key。
2. 将访问本站的域名加入该 Key 的域名白名单。
3. 在系统设置中选择“高德地图”，填写 Key 和配套安全密钥
   `securityJsCode`，点击“保存并应用”。
4. 返回首页检查地图。加载失败时，检查网络、Key 类型、域名白名单、
   配套安全密钥以及高德控制台中的服务配额。

保存过的凭据不会回显。留空表示保留旧值；更换 Key 时应同时填写
对应的安全密钥。网页 Key 会按高德 SDK 的要求传给已登录浏览器，
安全密钥由应用服务代理添加，不发送给浏览器。两者均加密存储，
不要更换现有部署的 `ENCRYPTION_KEY`。

地图使用与项目已有数据库函数一致的 WGS84 到 GCJ-02 转换，
原始位置和导出数据保持原样。地理围栏编辑器在高德地图中选点后，
会把显示坐标反向转换为 WGS84 再保存；打开或单独调整半径不会重写
原有中心点。统一平台的地图读取同一份已保存配置，旧采集器的
只读地图保留原有实现。

启用高德时，页面安全策略允许官方 SDK 和绘图模块域名
`webapi.amap.com`、`jsapi-service.amap.com`、`mapplugin.amap.com`，
以及 JS API 2.0 所需的
动态函数执行。内联脚本仍被限制；切换为 OpenStreetMap 后不再增加
高德的脚本权限。应用代理会校验高德 JSONP 的回调名和 JSON 内容，
以正确的 JavaScript 响应类型返回，同时保留 `nosniff` 保护。

HTML 响应为脚本生成每次请求独立的 CSP nonce，兼容 Cloudflare 自动
注入的检测脚本；页面的主题和应用脚本保持原有加载顺序。地图尺寸
由页面的 ResizeObserver 补充监听，旧浏览器退回窗口 resize 事件。
高德 SDK 内置监听使用的固定 `javascript:void(0)` 空操作仅按精确
SHA-256 哈希授权。兼容浏览器分别对完整 URL 或 `void(0)` 正文取哈希
的差异，其他未授权的内联脚本仍被限制。

## OpenStreetMap

无需 Key。国内网络可能无法连接其地图服务。深色模式只调整地图
瓦片的颜色，车辆标记、路线和控件保持清晰。更新前使用该提供商的
部署会保留现有选择，直到管理员保存新的配置。

地图服务商会接收显示地图所需的区域请求。高德的可用性仍取决于
服务商和网络；页面会在失败时显示提示和重新加载入口。

## 地理围栏

管理员打开“地理围栏”，为家、公司或充电站命名并设置范围。
范围内的行程起终点和充电记录会关联该地点，还可设置按电量或
时长计费以及每次充电的固定费用。已有费用不会因补算历史费用而被覆盖。

新建或编辑时，可搜索地址并选择结果，也可点击地图、拖动圆心和
圆边缘，或直接填写 1–4999 米的半径。所有修改点击“保存”后生效。
地图提供商和深浅色与系统设置一致，搜索也使用所选提供商。
高德地址查找仅向管理员开放；地图安全密钥仍只保存在服务器中。


参考：[高德安全密钥配置](https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode)、
[地图深浅色样式](https://lbs.amap.com/api/javascript-api-v2/guide/map/map-style)。
