# 账号、授权与数据隔离

## 功能位置

- 用户与权限：管理员保存「允许新用户注册」开关。首次升级默认关闭；关闭后注册页面和提交接口均拒绝新注册，已有账号不受影响。
- Tesla 连接：所有已登录用户都可使用。个人 Tesla OAuth 授权、配对钥匙和本账号车辆的遥测配置在这里管理。
- 地理围栏：所有已登录用户都可管理自己的地点名称与计费规则，地图跟随系统地图设置。
- 账号设置：个人资料、密码、两步验证和登录设备。兼容采集器的全局连接入口放在系统设置，仍限管理员。

## 车辆与围栏归属

每辆车只能绑定一个平台账号。官方授权回调只使用 Tesla 返回的车辆清单创建或绑定车辆，不能用提交的 VIN 或车辆 ID 领取他人车辆。已存在的其他账号归属不会被覆盖；管理员需要先核实，再撤销旧归属和分配新归属。管理员仍可查看车辆数据并管理系统。

每个账号独立保存加密的 Tesla access / refresh token。官网授权状态同时绑定发起登录的有效平台会话，过期、撤销和重复回调会失败。重新授权不会替换其他账号的凭据。解除个人授权保留车辆历史数据，撤销该授权产生的个人绑定并登出该平台账号的所有设备。

原有兼容采集器凭据保持独立。通过个人官方授权新增的车辆使用所属账号的 Fleet API，关闭旧 Streaming API，并以 30 秒作为初始轮询间隔。官方 API / 遥测用量由部署所使用的 Tesla 开发者应用承担，应在开发者后台设置适合部署规模的额度。

地理围栏按账号保存；查询、创建、修改、删除、历史关联和补算费用都限制在所属账号的车辆。重新分配车辆时重新计算地点关联，避免原账号的私有地点名称跟随车辆。已有实际充电费用不会被自动补算覆盖。

## 两步验证

使用标准 TOTP 验证器，在账号设置验证当前密码后手动添加密钥，再输入 6 位动态码确认。设置窗口为 10 分钟，只对发起设置的登录会话有效。密钥在数据库中加密保存，不通过第三方二维码服务传输。

动态码每 30 秒更新，接受当前及前一个时间段，成功使用后不能重放。登录挑战有效期为 5 分钟，最多 5 次尝试；账号的验证尝试也受到服务器持久化限流。输入密码后必须完成第二因素验证才建立完整登录会话。

启用后生成 10 枚单次恢复码，仅显示一次，数据库只存储哈希。关闭两步验证或重新生成恢复码需要密码和动态码 / 恢复码，且会失效旧会话。请在启用时保存恢复码；系统不会自动关闭两步验证。

## 登录设备

显示有效会话的设备类型、登录时间、最近活动时间和 IP，时间为北京时间。设备识别基于浏览器提供的信息，最近活动不等同于实时在线状态；同一浏览器的标签页共享会话。

会话令牌仅以哈希保存。只能列出和撤销自己的会话，可以登出某台设备或其他全部设备。撤销会话会通知对应 LiveView 连接断开；HTTP 请求、实时页挂载、事件、导航与数据推送都会再次校验会话。修改密码、停用账号或变更角色同样使旧会话失效。

## 升级保护

迁移新增账号安全与归属字段，不删除车辆历史。若旧安装存在同一车辆同时绑定多个用户的情况，唯一归属约束会拒绝迁移，必须先核实实际归属，不能静默删除绑定。部署前备份数据库及加密配置，并核对账号、车辆与历史记录。回滚涉及安全数据时应使用核验过的备份，不能直接删除新表来绕过验证。

验证覆盖注册状态与角色伪造、跨账号车辆 / 围栏 / 会话 ID、过期与撤销会话、动态码及恢复码重放、OAuth 会话撤销竞争、个人凭据隔离、接收器 VIN 归属，以及新车辆采集凭据的选择。
