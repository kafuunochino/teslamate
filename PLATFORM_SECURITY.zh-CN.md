# 多用户平台权限与安全模型

本文说明统一平台的信任边界、权限规则和当前限制。它是部署与代码审查依据，不替代主机、数据库和反向代理的安全加固。

## 角色与能力

| 能力                                        | 未登录 |   普通用户 |    管理员 |
| ------------------------------------------- | -----: | ---------: | --------: |
| 注册账号（开关启用时）                      |     是 |          — |         — |
| 登录、修改自己的资料/密码                   |     否 |         是 |        是 |
| 查看车辆总览、位置、轨迹、电池、充电、分析  |     否 | 仅绑定车辆 |  全部车辆 |
| 导出 GPX                                    |     否 | 仅绑定车辆 |  全部车辆 |
| 兑换一次性车辆认领码                        |     否 |         是 | UI 不提供 |
| 解除自己的车辆绑定                          |     否 |         是 |    不适用 |
| 授予/撤销车辆权限                           |     否 |         否 |        是 |
| 生成/撤销认领码                             |     否 |         否 |        是 |
| 修改用户角色和状态                          |     否 |         否 |        是 |
| Tesla token、采集控制、设置、地理围栏、导入 |     否 |         否 |        是 |

最后一个有效管理员不能被停用或降级。角色或账号状态变化会删除该账号的全部平台会话；管理员 LiveView 每次写操作还会重新查询数据库中的管理员状态，避免已打开页面保留旧权限。

## 身份边界

平台身份与 Tesla 采集身份分离：

- 平台用户保存在 `private.users`，只用于网站登录和车辆数据授权；
- Tesla access/refresh token 仍由原 `private.tokens` 与 `ENCRYPTION_KEY` 管理；
- 普通用户不能读取 token、不能连接 Tesla 账号，也不能调用车辆采集控制接口；
- 管理员查看全部车辆是平台 RBAC 能力，不会把 Tesla token 发送到浏览器。

## 查询授权

`TeslaMate.Fleet` 是统一页面的只读查询边界。每个公开查询都接收当前用户：

1. 从数据库重新判断账号是否仍为有效管理员；
2. 管理员查询全部 `cars`；
3. 普通用户通过 `private.user_cars` 与有效用户状态连接后得到允许的 `car_id`；
4. 行程、位置、充电和分析查询只使用这些 `car_id`；
5. 行程详情和 GPX 先执行同一授权，再加载采样点。

这避免只在菜单中隐藏车辆、但仍能通过修改 URL 访问其他车辆的 IDOR 漏洞。控制器和 LiveView 不能自行按外部 `car_id` 直接查遥测表。

## 车辆认领

平台不允许通过 VIN、车辆名称、车牌、邮箱域名或“第几辆车”自助认领。

- 认领码由管理员为一辆明确车辆生成；
- 原文包含 192 位随机熵，使用 URL-safe Base64；
- 数据库只保存 SHA-256 哈希；
- 原文仅在创建响应中显示一次；
- 有效期最短 1 小时、最长 72 小时；
- 兑换使用数据库行锁，同一码只能成功一次；
- 已使用、已撤销或已过期的码不能再次兑换；
- 创建、兑换和撤销写入 `private.audit_events`。

认领码本身是临时凭据。管理员必须通过端到端加密或其他可信渠道发送，不能放进公开聊天、工单截图或日志。

## 密码与会话

- 密码长度 12–128 字符；
- 使用 PBKDF2-HMAC-SHA256、每用户 16 字节随机盐、600,000 次迭代、32 字节派生值；
- 登录对不存在的用户也执行一次固定 PBKDF2，降低明显的时序枚举；
- 登录与注册共用按 IP/邮箱的滑动窗口速率限制，默认 10 分钟内单 IP 10 次、单邮箱 5 次；
- 会话 token 使用 32 字节随机值，数据库仅保存 SHA-256 哈希；
- 会话默认 30 天，最大 90 天；
- 修改密码、角色或状态会撤销全部会话；
- 生产 Cookie 为 `Secure`、`HttpOnly`、`SameSite=Strict`；
- 浏览器表单使用 Phoenix CSRF token；管理 API 还要求有效管理员和同源检查。

速率限制当前保存在单实例 ETS 中，重启后会清空，也不会跨多个副本同步。当前 Compose 只运行一个 Web 实例；若以后做水平扩展，应改为共享的限流存储或在反向代理/WAF 增加限制。

## 数据库迁移与回滚

多用户迁移只在已有 `private` schema 中新增五张表。外键引用原 `public.cars`，但不会改写原遥测表。

- `private.users`：账号、角色和状态；
- `private.user_sessions`：哈希会话；
- `private.user_cars`：用户与车辆的显式多对多授权；
- `private.vehicle_claims`：哈希认领码及生命周期；
- `private.audit_events`：权限安全事件。

旧代码会忽略这些新增表，因此普通代码回滚不需要删表或恢复数据库。任何数据库恢复都会丢失备份后的新遥测，必须单独审批。

## 网络边界

- 宿主机只发布 `127.0.0.1:3000:3000`；
- 4000 不发布；
- PostgreSQL 与 MQTT 不发布；
- 旧 Grafana profile 默认不启动，即使启动也不发布宿主机端口；
- 公网必须经 HTTPS 反向代理；
- `CHECK_ORIGIN` 应设为真实 HTTPS Origin；
- `TESLAMATE_TRUSTED_PROXIES` 只允许实际反向代理 IP/CIDR，不能信任任意来源的 `X-Forwarded-For`；
- 生产环境必须固定保存 `ENCRYPTION_KEY`、`SECRET_KEY_BASE` 和 `SIGNING_SALT`。

## 浏览器安全头

统一页面默认发送 CSP、`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: same-origin` 和禁用摄像头/麦克风/定位权限的 `Permissions-Policy`。CSP 不允许第三方脚本或 iframe；样式因 Leaflet 地图和动态图表仍允许 inline style。

地图当前从 OpenStreetMap 请求瓦片。瓦片服务会看到客户端 IP、时间和所请求的瓦片坐标，从而推断近似区域；平台不会把用户邮箱或 VIN放入请求。对位置隐私要求更高的部署应使用 VPN 出口或后续接入自托管瓦片。

## 审计事件

数据库审计包含注册、成功登录、密码变化、角色/状态变化、车辆授权/撤销、用户解绑、认领码创建/兑换/撤销。密码、会话原文、Tesla token 和认领码原文不会写入审计 metadata。

失败登录与限流事件另外写入应用日志和有界 ETS 缓冲区。日志只显示掩码邮箱；部署方应把容器日志送入受保护的集中日志系统并设置保留期。

## 当前明确限制

- 没有邮件验证、邀请邮件和自助密码找回；管理员需线下核实用户身份。
- 没有 MFA/Passkey；管理员应使用独立强密码，并把管理入口限制在可信网络或额外身份网关后。
- 车辆授权是只读数据权限；系统尚未向普通用户提供 Tesla 车辆控制。
- 分析评分是透明规则统计，不是安全评价、保险评分、BMS 健康报告或维修诊断。
- 单实例限流重启即清空。
- 第三方地图瓦片存在上述位置元数据披露。
- 管理员是高信任角色，可以查看所有精确轨迹；必须严格控制管理员数量并定期审计。

## 发布前验收清单

- [ ] 外部 PostgreSQL 已做可恢复备份并通过 `pg_restore --list`。
- [ ] 3000 只监听 `127.0.0.1`，4000 未监听。
- [ ] 公网域名强制 HTTPS，WebSocket 可用。
- [ ] 三个密钥为独立随机值且未进入 Git。
- [ ] 首个管理员已创建，最后管理员保护测试通过。
- [ ] 新普通用户默认零车辆。
- [ ] 单车授权后无法访问其他车辆的首页、行程详情和 GPX。
- [ ] 撤销授权或停用账号后会话失效。
- [ ] 同一认领码第二次兑换失败。
- [ ] 遥测核心表行数未减少。
- [ ] `GRAFANA_VOLUME_NAME` 等于旧容器的真实挂载卷，且该卷仍存在、未被重建。
